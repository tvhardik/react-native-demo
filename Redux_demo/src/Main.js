import {View, Text} from 'react-native';
import React from 'react';
import Tab from './Navigation/Tab';

const Main = () => {
  return (
    <View style={{flex: 1}}>
      <Tab />
    </View>
  );
};

export default Main;
